name: YouTube Shorts & Longform Auto Upload

on:
  schedule:
    # ⏰ 시간대 변환: KST - 9시간 = UTC
    # 🎬 쇼츠: 매일 3회 / 롱폼: 매일 1회 (17:30 KST만)
    - cron: '30 2 * * *'     # 매일 02:30 UTC = 11:30 KST (쇼츠만)
    - cron: '30 8 * * *'     # 매일 08:30 UTC = 17:30 KST (쇼츠 + 롱폼)
    - cron: '30 12 * * *'    # 매일 12:30 UTC = 21:30 KST (쇼츠만)
  
  workflow_dispatch:  # 수동 실행 가능
    inputs:
      type:
        description: 'Video type to generate'
        required: true
        default: 'shorts'
        type: choice
        options:
          - 'shorts'
          - 'longform'
          - 'both'
      count:
        description: 'Number of videos to generate (shorts only)'
        required: false
        default: '1'
        type: choice
        options:
          - '1'
          - '2'
          - '3'
      upload:
        description: 'Enable YouTube upload'
        required: true
        default: 'true'
        type: choice
        options:
          - 'true'
          - 'false'

env:
  UPLOAD_ENABLED: ${{ github.event.inputs.upload || 'true' }}
  VIDEO_COUNT: ${{ github.event.inputs.count || '1' }}

jobs:
  create-and-upload:
    runs-on: ubuntu-latest
    
    steps:
      - name: Determine video type
        id: set-type
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            # 수동 실행: 사용자 선택값 사용
            echo "VIDEO_TYPE=${{ github.event.inputs.type }}" >> $GITHUB_ENV
          elif [ "${{ github.event.schedule }}" = "30 8 * * *" ]; then
            # 17:30 KST: 쇼츠 + 롱폼
            echo "VIDEO_TYPE=both" >> $GITHUB_ENV
          else
            # 11:30, 21:30 KST: 쇼츠만
            echo "VIDEO_TYPE=shorts" >> $GITHUB_ENV
          fi

      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      
      - name: Install FFmpeg and Korean fonts
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg
          
          # Noto Sans CJK 폰트 (권장)
          sudo apt-get install -y fonts-noto-cjk fonts-noto-cjk-extra
          
          # Nanum 폰트 (대체용)
          sudo apt-get install -y fonts-nanum fonts-nanum-coding
          
          # 폰트 캐시 업데이트
          sudo fc-cache -fv
          
          # 설치된 한글 폰트 확인
          echo "=== 설치된 한글 폰트 ==="
          fc-list | grep -i "noto\|nanum" || echo "No fonts found"
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Setup credentials
        run: |
          mkdir -p config
          echo '${{ secrets.YOUTUBE_CLIENT_SECRETS }}' > config/client_secrets.json
          
          # 채널 ID에 맞는 인증 파일명 생성 (YouTubeUploader의 네이밍 규칙 일치)
          echo '${{ secrets.YOUTUBE_CREDENTIALS }}' > config/youtube_credentials.json
          echo '${{ secrets.YOUTUBE_CREDENTIALS }}' > config/youtube_credentials_L4y1Qbdg.json
          
          # Update config with API keys
          python -c "
          import json
          with open('config/config.json', 'r') as f:
              config = json.load(f)
          gemini_key = '${{ secrets.GEMINI_API_KEY }}'
          if gemini_key:
              config['gemini_api_key'] = gemini_key
          # HuggingFace 토큰 (시크릿이 있으면 사용, 없으면 config.json 값 유지)
          hf_token = '${{ secrets.HUGGINGFACE_TOKEN }}'
          if hf_token:
              config['huggingface_token'] = hf_token
          # Together AI API 키
          together_key = '${{ secrets.TOGETHER_API_KEY }}'
          if together_key:
              config['together_api_key'] = together_key
          # Pexels API 키
          pexels_key = '${{ secrets.PEXELS_API_KEY }}'
          if pexels_key:
              config['pexels_api_key'] = pexels_key
          upload_flag = '${{ env.UPLOAD_ENABLED }}'
          config['scheduler']['upload_enabled'] = (upload_flag == 'true')
          with open('config/config.json', 'w') as f:
              json.dump(config, f, indent=2)
          "
      
      - name: Create video
        run: |
          if [ "${{ env.UPLOAD_ENABLED }}" = "true" ]; then
            python main.py --type ${{ env.VIDEO_TYPE }} --count ${{ env.VIDEO_COUNT }}
          else
            python main.py --type ${{ env.VIDEO_TYPE }} --count ${{ env.VIDEO_COUNT }} --no-upload
          fi
      
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: generated-video-${{ github.run_number }}
          path: |
            output/videos/*.mp4
            output/longform_videos/*.mp4
            output/thumbnails/*.jpg
            output/images/*.png
            output/longform_images/*.jpg
            logs/*.json
          retention-days: 7
