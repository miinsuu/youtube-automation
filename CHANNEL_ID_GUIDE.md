# ⚠️ YouTube 채널 ID 설정 가이드

## 문제: 영상이 다른 채널에 업로드됨

여러 YouTube 채널을 소유하고 있을 때, **설정한 채널이 아닌 다른 채널에 영상이 업로드되는 문제**가 발생할 수 있습니다.

### 원인

YouTube Data API v3의 `/videos.insert` 메서드는 **현재 로그인한 계정의 기본 채널(Primary Channel)로만 업로드**합니다.

즉:
- `target_channel_id` 설정은 **참고용**일 뿐
- 실제 업로드 대상은 **인증에 사용된 Google 계정의 기본 채널**
- 여러 채널을 관리하는 경우, 인증 방식에 따라 어느 채널로 업로드될지 결정됨

---

## 🔧 해결 방법

### **방법 1: 인증 계정 확인 (권장)**

1. **현재 로그인된 계정 확인**
   ```bash
   python main.py --count 1
   ```

2. **로그 출력 확인**
   ```
   ✓ 현재 로그인 채널: [채널 이름] ([채널 ID])
   ```

3. **실제 업로드되는 채널이 이 채널인지 YouTube에서 확인**

### **방법 2: 올바른 계정으로 재인증**

1. **기존 인증 정보 삭제**
   ```bash
   rm config/youtube_credentials.json
   ```

2. **목표 채널을 기본 채널로 설정한 Google 계정으로 재인증**
   ```bash
   python main.py --count 1
   ```

3. **인증 과정에서 나타나는 브라우저에서 올바른 계정으로 로그인**

### **방법 3: 브랜드 계정 기본 채널 변경 (Google Account 수준)**

여러 채널을 관리하는 경우, Google 계정 설정에서 기본 채널을 변경할 수 있습니다:

1. **YouTube 채널 스위처**
   - YouTube.com 우측 상단 프로필 아이콘 클릭
   - "채널 만들기" 아래 채널 목록 확인
   - 기본으로 설정할 채널을 선택

2. **또는 YouTube Studio**
   - https://studio.youtube.com 방문
   - 좌측 하단 채널 선택 드롭다운
   - 기본 채널 설정

---

## 📋 설정 파일 확인

`config/config.json`에서 채널 ID 확인:

```json
{
  "youtube": {
    "target_channel_id": "UC2yneYUgVE2VSzRL4y1Qbdg"
  }
}
```

### 내 채널 ID 찾기

1. **YouTube Studio 방문**
   - https://studio.youtube.com

2. **설정 > 기본 정보**
   - 채널 ID 복사

3. **또는 YouTube 채널 URL에서**
   - https://www.youtube.com/channel/`UC2yneYUgVE2VSzRL4y1Qbdg`
   - `UC`로 시작하는 부분이 채널 ID

---

## ✅ 확인 방법

### **업로드 후 확인**

```bash
python main.py --count 1
```

실행 로그에서 다음 확인:

```
✓ 현재 로그인 채널: [내 채널 이름] ([내 채널 ID])
🎯 업로드 대상 채널: UC2yneYUgVE2VSzRL4y1Qbdg
✅ 업로드 완료!
   비디오 ID: [영상 ID]
   URL: https://www.youtube.com/watch?v=[영상 ID]
```

**URL 방문** → 우측 채널 아이콘 클릭 → 채널명 확인

---

## 🔐 GitHub Actions에서의 설정

### **Secrets 설정 확인**

`.github/workflows/youtube-automation.yml`에서 현재 계정 확인 로직이 추가되었습니다:

```yaml
- name: Generate and Upload Video
  run: |
    python main.py --count 1
```

**GitHub Actions 실행 로그 확인:**
1. GitHub Repository > Actions 탭
2. 최신 실행 선택
3. "Generate and Upload Video" Step 확인
4. 로그에서 현재 로그인 채널 확인

### **여러 채널에 업로드하려면?**

현재는 하나의 기본 채널로만 업로드 가능합니다. 여러 채널로 업로드하려면:

1. **여러 파이썬 인스턴스 실행** (각각 다른 인증)
2. **또는 Custom Channel API 개발** (향후 업데이트 예정)

---

## 📊 채널 ID 검증

### 채널 ID 형식

- **정상**: `UC`로 시작 + 24자 (예: `UC2yneYUgVE2VSzRL4y1Qbdg`)
- **비정상**: `@` 로 시작하는 핸들 (예: `@mychannel`)

### 내 채널 ID 확인 Python 스크립트

```python
from scripts.youtube_uploader import YouTubeUploader

uploader = YouTubeUploader()
if uploader.authenticate():
    channels = uploader.get_my_channels()
    for ch in channels:
        print(f"채널: {ch['title']}")
        print(f"ID: {ch['channel_id']}")
        print()
```

---

## 🆘 트러블슈팅

### **문제: "업로드는 되는데 다른 채널에 올라옴"**

**원인**: 인증 계정이 다름

**해결:**
1. 로그에서 현재 로그인 채널 확인
2. 목표 채널을 기본 채널로 설정한 계정으로 재인증
3. `rm config/youtube_credentials.json` 후 재시도

### **문제: "업로드 권한 없음" 오류**

**원인**: API 권한 미설정

**해결:**
1. Google Cloud Console 확인
2. YouTube Data API v3 활성화
3. OAuth 2.0 클라이언트 ID 생성 확인

### **문제: 여러 채널 중 어느 채널인지 확실하지 않음**

**확인:**
```bash
python -c "from scripts.youtube_uploader import YouTubeUploader; u = YouTubeUploader(); u.authenticate(); ch = u.get_authenticated_channel(); print(f'채널: {ch[\"title\"]} ({ch[\"id\"]})')"
```

---

## 📞 추가 정보

- **YouTube Data API**: https://developers.google.com/youtube/v3
- **OAuth 설정**: https://console.cloud.google.com/apis/credentials
- **채널 관리**: https://studio.youtube.com/channel/

---

**마지막 업데이트**: 2026년 2월 13일
**버전**: 2.0 (채널 ID 검증 추가)
